# Handoff — 2609 JC covariances deck, 2 Sep 2026

Build: `quarto render 2609_JC_covariances.qmd -M embed-resources:true` (embed is required —
Cail's viewer does not resolve sibling assets), then copy `../_site/.../index.html` to
`~/loom/.felt/work/talks/2609-jc-covariances/deck.html`. 20 visible slides + 2 parked.

## Slides (deck number → `data-step`)

3–4 field / sum of modes → 0, 1 · 5 mask convolves the modes → 2 · 6 bandpowers → 3 ·
7 the covariance is expensive → 4, `data-frag-step=5` · 8 "what is your mask?" → 6 ·
9 band-limiting the mask → 6 (same ledger as 8) · 10 splitting the pairs → 8 ·
11–13 validations + NaMaster → 9 · 15 pair count → 10 · 16 the projection → 10.
Slides 1, 2, 14 (title, two paper cards) carry no step. 17–20 untouched this session.
`data-staged` on 4, 5, 8, 10, 15: stage = fragment index + 1, read by ledger `sub`.

## Engine (`nka_script.html`) — what slide 16 rests on

- `covariances()` fills three NK×NK matrices (NK = 150): `covE` exact 1D Gaussian
  covariance, `covN` NKA with C̄ = ½(C_ℓ+C_ℓ′), `covI` iNKA with ½(C̃_ℓ+C̃_ℓ′) against the
  same mask-product kernel. `Ctil` is stored already divided by `sumP` = ⟨w²⟩ (Parseval) —
  dividing by `MW2` again squares the mask fraction and saturates every panel.
- `projKernel()` / `project(cov)`: the 1D Legendre analogue, T[i][ℓ] = 2cos(2πℓθ_i),
  NTH = 22 bins over 0.05 of the box. Cov(ξ) = T · Cov(C̃) · Tᵀ.
- `projSet()` caches the nine panels. `normBy`/`diagOf` give correlation matrices
  normalised by the EXACT diagonal; `diffNoDiag` zeroes the diagonal.
- `drawProj(root)` paints canvases by class: `hFsky cFsky dFsky hInka cInka dInka cQuar dQuar`.
  `heatN(cv, mat, n, scale)` is `heat()` for any n; `heat()` stays NK-only.
- `PROJ = null` in `buildAll()` — slide 7's mask picker rebuilds slide 16 too.
- Ledger `hold:true` renders an entry one step early with `visibility:hidden`, so its box
  exists from the base state and nothing below the ledger moves when a fragment fires.
- `.caption` (28 px) is the one caption size in the deck.

## QA

- `node _qa.mjs <abs html> screenshots/<pfx> <idx0>[,…] <maxFrags>` — worst bottom pixel per
  fragment in SLIDE coordinates; keep < 1080. Ten slides per run; twenty exceeds the 120 s
  Bash timeout. `_qa8.mjs` sweeps slide 9's θc slider, `_qa7.mjs` slide 7's three masks.
- Motion: measure `getBoundingClientRect()` of every cell across fragments and compare the
  strings. That is how `hold` was verified.
- Collision: rectangle intersection between a label and every other box.
- LOOK at the PNGs. Three real defects this session were invisible to the numbers.

## Pitfalls that cost time

- `.nka svg { display:block }` also hits MathJax's inline SVGs; the `mjx-container svg
  { display:inline }` exception is load-bearing.
- `drawMatrices()` grabbed every canvas on any slide; now scoped to `.mrow canvas`.
- 300 consecutive `mulberry` draws are sub-Poisson; catalogue positions use `xs128`.
- The figure-treatment helper multiplies against the PARENT's background, so an image wider
  than `.cardwrap` gets white bars. Card images cap at 1700 px.
- The Nagura card is a desaturated arXiv-page capture. The PDF cannot carry title and a
  ≥28 px abstract: at 300 dpi the abstract block alone needs 814 of the card's ~900 px.
- Slicing `custom.scss` between comment markers once deleted the paper-card block.

## Slide 16, next hour

1. Matrices are 208 px; the brief wanted ≥260. Try the three equations stacked above and a
   3×3 matrix grid below, or split into two slides.
2. The iNKA residual is faint at the default "small" mask. Default the deck to `pick = 2`
   ("holes"), or draw slide 16 always with the holed mask — the paper's case is a
   complicated mask, and that is where iNKA fails.
3. The Δ column's colour scale (×0.5) is unstated.
4. The quartic row's zero Δ is a blank bordered box; it may read as missing, not zero.
